<!-- skillfile:generated -->
# skillfile

Generate and keep fresh cross-agent context files (AGENTS.md, SKILL.md) from your repo, deterministically. No LLM calls, no API key.

## Stack

- Language: JavaScript/TypeScript
- Package manager: npm

## Commands

- `test`: `node test/smoke.js`
- `check-tools`: `node scripts/check-tools.js`
- `sync-site`: `node scripts/sync-site.js`
- `k`: `node scripts/k-metric.js --append`

## Structure

- `.claude-plugin/`
- `.github/`
- `bin/`
- `scripts/`
- `site/`
- `skills/`
- `src/`
- `test/`
- `test-fixtures/`

## Tooling

- CI configured (`.github/workflows`)
- Has a dedicated test directory

## Recent activity

Branch: `main`
- Cut the wording down and make it feel solid
- Draw the mechanism instead of describing it
- Replace the prose with a machine you step through
- Rebuild the page as the artifact it produces
- Mark the community submission as sent

---
Generated by [skillfile](https://skillfile.pro) — run `npx skillfile update` after structural changes, or `npx skillfile check` in CI to catch drift.
